#~ sys.source("~/.Rprofile")

if(!("devtools" %in% getOption("defaultPackages")))
    options(defaultPackages=c(getOption("defaultPackages"), "devtools"))
if(!("testthat" %in% getOption("defaultPackages")))
    options(defaultPackages=c(getOption("defaultPackages"), "testthat"))
if(!("covr" %in% getOption("defaultPackages")))
    options(defaultPackages=c(getOption("defaultPackages"), "covr"))

